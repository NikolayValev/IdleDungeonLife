// Stub for future scrollable panel component
// For MVP, scenes use simple vertical layout with overflow guard

export function makeScrollablePanel(_scene: Phaser.Scene, _x: number, _y: number, _w: number, _h: number): void {
  // Placeholder — implement with Phaser mask/camera for V2
}
